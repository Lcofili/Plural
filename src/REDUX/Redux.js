import React from "react";
import styled from "styled-components";
// import HomeSceen from "../REDUX/HomeSceen";

const Redux = () => {
  return (
    <Container>
      <Wrapper></Wrapper>
    </Container>
  );
};

export default Redux;

const Container = styled.div``;
const Wrapper = styled.div``;
